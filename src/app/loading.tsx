import { DeveloperIcon } from '@/components';

const Loading = () => (
  <div className='flex flex-col justify-center items-center h-screen'>
    <DeveloperIcon icon='/loading.json' />
  </div>
);

export default Loading;
